version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist   # Cambia 'build' por 'dist'
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*